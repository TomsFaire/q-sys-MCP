# Dependencies
node_modules/
# Ignore lockfiles except under web/ (reproducible UI installs)
package-lock.json
!web/**/package-lock.json
yarn.lock

# Build output
dist/
*.tsbuildinfo

# Environment
.env
.env.local
.env.*.local

# IDE / local tooling
.vscode/
.idea/
.cursor/
.claude/
*.swp
*.swo
*~
.DS_Store
Icon*

# Internal planning docs (local only)
docs/
plan.md
CLAUDE.md
# Cursor / exported planning specs (if copied into the repo)
*.plan.md

# Worktrees (isolated development branches)
.worktrees/

# Logs
*.log
npm-debug.log*
